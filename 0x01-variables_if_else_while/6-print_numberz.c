#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
 * main - This program prints the single number from 1 to zero
 *
 * Return: 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
