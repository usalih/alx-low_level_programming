#include "main.h"
/**
 * main - check the code
 *
 * return: Always 0
 */
int main(void)
{
	char *str;

	str = "i do not fear computer. i fear the lack of them - Isaac Asimov";
	print_rev(str);
	return (0);
}
